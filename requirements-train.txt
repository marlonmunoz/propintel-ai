# Training + pipeline dependencies — NOT installed in the API container.
# The Docker image uses requirements-api.txt (see Dockerfile).
# Install this file for local ML work:
#   pip install -r requirements-train.txt
#
# Includes everything in requirements-api.txt plus the packages below
# that are only needed for training, evaluation, and data pipelines.

-r requirements-api.txt

catboost==1.2.10
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
graphviz==0.21
kiwisolver==1.5.0
matplotlib==3.10.8
narwhals==2.18.0
openpyxl==3.1.5
pillow==12.1.1
plotly==6.6.0
pyarrow==24.0.0
scipy==1.17.1
tqdm==4.67.3
